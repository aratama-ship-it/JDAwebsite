.DS_Store
Thumbs.db

# Local editor and OS noise
.idea/
.vscode/
*.swp
*.swo

# Temporary files and local logs
*.log
tmp/
temp/

# Dependency/build folders if this static mock gains tooling later
node_modules/
dist/
build/
.cache/
.superdesign/tmp/

# Python helper artifacts
__pycache__/
*.py[cod]
.venv/
venv/

# iCloudの同期競合コピー(「app 2.js」等)は追跡しない
* [0-9].*

# 会議記録・内部メモは公開リポジトリに置かない（meeting-prep/ へ）
docs/meetings/

# Cyberduckへ渡すローカル生成物。GitHubにはソースと生成スクリプトだけを保存する
/release-candidate/

# 本番反映前に取得する現行ファイルの控え（ロールバック用）。生成スクリプトだけを追跡する
/backups/

# 制作の実験・生成素材。サイトからは参照されず、容量が大きいので追跡しない。
# 削除はせずローカルに残す（再生成の手順は各フォルダのメモを参照）。
/prototypes/champion-card-studio/
/design-experiments/
/images/generated/

# 旧公開フローが入力に使う、検査済み旧docrootのスナップショット
/work/jda-approved-legacy-docroot/

# SuperDesignの作業用データ
/.superdesign/
